<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WingetStore.Pages.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:WingetStore.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:WingetStore.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <controls:ResponsivePageContainer>
        <Grid RowSpacing="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="About App" />

            <StackPanel Grid.Row="1" Spacing="20" MaxWidth="600" HorizontalAlignment="Left">
                <Border
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="24">
                    <StackPanel Spacing="16" HorizontalAlignment="Center">
                        <Border
                            Width="80"
                            Height="80"
                            CornerRadius="16"
                            Background="{ThemeResource AccentFillColorDefaultBrush}"
                            HorizontalAlignment="Center">
                            <FontIcon Glyph="&#xE7B8;" Foreground="White" FontSize="40" />
                        </Border>
                        
                        <StackPanel Spacing="4" HorizontalAlignment="Center">
                            <TextBlock Text="WinForge" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" />
                            <TextBlock Text="Version 1.0.0" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center" />
                        </StackPanel>

                        <TextBlock
                            Text="A fast, native, and modern Windows PowerTools suite combining a full-featured Package Manager Storefront, Windows Feature Velocity Management (ViVeTool), and deep System Performance Optimization."
                            FontSize="13"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            TextAlignment="Center"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </Border>

                <Border
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="20">
                    <StackPanel Spacing="12">
                        <TextBlock Text="Project Details" FontSize="15" FontWeight="SemiBold" />
                        <TextBlock Text="• Native WinUI 3 (Windows App SDK 2.3.1)" FontSize="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock Text="• Targets .NET 9 Windows Runtime" FontSize="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock Text="• Asynchronous background process pipe architecture" FontSize="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock Text="• Built in collaboration with Antigravity AI pair programming" FontSize="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </controls:ResponsivePageContainer>
</Page>
